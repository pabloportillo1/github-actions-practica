on: push
jobs:
  run-python:
    runs-on: ubuntu-latest
    container: python:3.11
    steps:
      - run: echo "TESTING"
      - run: python --version
      - uses: actions/checkout@v3
      - run: python script.py